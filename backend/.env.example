DATABASE_URL=postgresql+asyncpg://smartfactory:smartfactory@postgres:5432/smartfactory
SECRET_KEY=troque-isso-em-producao-use-openssl-rand-hex-32
ACCESS_TOKEN_EXPIRE_MINUTES=480
REFRESH_TOKEN_EXPIRE_DAYS=30
ALGORITHM=HS256
ENVIRONMENT=development
CORS_ORIGINS=http://localhost:3000

# StudyOS — opcional. Sem ANTHROPIC_API_KEY os agentes 07-11, 13 e 16 operam
# no modo determinístico (sem redator conectado); com a chave, passam a
# gerar conteúdo real via app/studyos/redator.py.
ANTHROPIC_API_KEY=
STUDYOS_REDATOR_MODELO=claude-sonnet-5
